{
  "schemaVersion": 1,
  "id": "kairos.plugin-analytics",
  "name": "Plugin Analytics",
  "version": "1.0.0",
  "author": "kairos",
  "license": "MIT",
  "description": "Developer analytics for your Omarchy marketplace plugins: hourly tracking of views, copies, hearts, stars and rank, with per-plugin breakdowns over 24h to all-time windows.",
  "kinds": ["bar-widget"],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Plugin Analytics",
    "description": "Marketplace stats for the plugins you publish",
    "category": "Info",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "author": "",
      "barMetric": "views",
      "barWindow": "24h",
      "defaultWindow": "7d",
      "autoTimer": true
    },
    "schema": [
      {
        "key": "author",
        "type": "string",
        "label": "Author",
        "defaultValue": "",
        "description": "Your marketplace author name or GitHub owner, exactly as listed in the catalog."
      },
      {
        "key": "barMetric",
        "type": "enum",
        "label": "Bar metric",
        "options": ["views", "copies", "hearts"],
        "defaultValue": "views",
        "description": "Which metric's delta the bar label shows."
      },
      {
        "key": "barWindow",
        "type": "enum",
        "label": "Bar window",
        "options": ["24h", "7d"],
        "defaultValue": "24h",
        "description": "The window the bar label's delta covers."
      },
      {
        "key": "autoTimer",
        "type": "boolean",
        "label": "Persistent collection",
        "defaultValue": true,
        "description": "Install and enable a user-scope systemd timer (plus user lingering) so snapshots continue through logouts, reboots and shell crashes."
      },
      {
        "key": "defaultWindow",
        "type": "enum",
        "label": "Default panel window",
        "options": ["24h", "7d", "30d", "90d", "180d", "365d", "all"],
        "defaultValue": "7d"
      }
    ]
  }
}
